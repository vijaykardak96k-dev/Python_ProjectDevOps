apiVersion: v1
kind: Service
metadata:
  name: smart-farm-frontend
  namespace: smart-farm
  labels:
    app: smart-farm
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: smart-farm
    component: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
